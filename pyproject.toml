[project]
name = "test-case-garda"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "mypy==1.19.*",
    "pre-commit==4.5.*",
    "ruff==0.15.*",
]
